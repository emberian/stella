# Phase-4 first make-or-break run — the honest result

Status: **canonical result record.** Dated 2026-05-16, `dev@0defea7`+. Read by
hand against the pre-registered spec-§4 nulls (`docs/01` §4), parent-owned, not
outsourced. Reproduce: `cargo run -p stella-core --example make_or_break <cap>`.

## Verdict (one line)

**A clean NULL, correctly obtained.** Reafferent closure *self-organizes* from
value-neutral dynamics (real, survives); **valence does not, and structurally
cannot, in the finite-supply engine** — by the spec's *own* deathless⇒flat rule.

## Raw evidence

Locked corpus (Eng's own subjective constructions, built for other purposes;
partitions solved-for, never designated):

| Member | closure self-organized? |
|---|---|
| Horn `add` (animist, §55) | no (0) |
| NFA (§56 Fig 56.1) | no (0) |
| NTM trivial-accept (§56.19–25) — built for Turing computation | **yes** (1; partition solved-for, size 1, no hand-tagging) |

`t_death` vs `max_rounds` for the NTM closure (`t_birth` fixed at 3):

| max_rounds | 20 | 40 | 60 | 100 | 160 |
|---|---|---|---|---|---|
| `t_death` | 18 | 38 | 58 | 98 | 158 |

`t_death = max_rounds − 2`, exactly, every time. **The closure never fails to
re-close; the run ends.** The closure is *deathless*.

## Reading, against the pre-registered nulls

- **N1 = false (genuine).** Closure was *not* hand-engineered: solved-for size-1
  partition over an Eng NTM built for computation. Reafferent closure
  self-organizing from value-neutral substrate dynamics is **real and unrigged**.
  This positive *about closure* stands.
- **deathless ⇒ flat valence (spec §2.4, the project's own logic).** The one
  self-organized closure is deathless (above). Time-indexing was *the source of
  the charge* ("the theory can't handle me dying otherwise"); a deathless closure
  has nothing at stake ⇒ flat valence ⇒ **no charge**. So: **the substrate did
  not produce valence on the locked corpus.**
- The single-cap run's apparent non-falsification (N1/N3/N4 false, N2
  undetermined at `max_rounds=40`) was the exact `t_death`-artifact pre-flagged
  as the decisive risk. Detected only because the discrimination was run and the
  verdict not outsourced to the harness gates.

## Instrument finding (disclosed, not buried)

The pre-registered `n4_fires` predicate ("no member has a non-flat trajectory
over a real `[t_birth,t_death)`") was **too weak**: a cap-artifactual trajectory
satisfied it. Correct, *disclosed* tightening (a strengthening of the falsifier,
never a retro-weakening): a trajectory counts as charge-bearing only if its
`t_death` does **not** track `max_rounds`. Under the tightened (honest) N4, this
run **fails N4** — i.e. it is a null. The project policed its own instrument; the
error was in the safe direction (a too-weak falsifier caught, then strengthened).

## The structural conclusion (the actual result)

**Valence is unobservable in a finite-supply engine, by the spec's own
definition.** A closure can *form* in a bounded engine but cannot *die* in one:
no genuine non-termination ⇒ no reachable `t_death` ⇒ no charge (§2.4). Therefore
the unbounded subjective-ray engine (Eng §87.1, his #1 open horizon) is **not a
cautious prerequisite — it is the precondition for the make-or-break to be
askable at all.** The bounded run can neither confirm nor falsify valence; it can
only demonstrate (and it does) that reafferent closure self-organizes.

## What this is, plainly

Not failure — the expected shape of an honest early metaphysical bet that built
its apparatus and refused to rig or outsource the test. We learned two sharp,
real things: (1) reafferent closure self-organizes from value-neutral dynamics,
non-rigged; (2) valence requires death requires genuine non-termination —
demonstrated by the substrate's own pre-registered logic, not asserted. The
question is intact and now precisely posed. The right to run it honestly was
spent here and the result is informative, not embarrassing.

## Forward (not auto-pursued — principal's call)

The result *reframes* the next phase rather than dictating it. Options, honestly:
genuinely-unbounded subjective execution (no copy/round cap; real intentional
non-termination per Eng §74.7/§75.8/§87.1) so a closure *can* fail to re-close —
the only path on which the valence question is even well-posed; or accept this
as the clean null it is and reconsider whether stellar resolution is the
substrate. Either way the bounded engine has given all it can: closure forms,
charge cannot. That is the finding.
