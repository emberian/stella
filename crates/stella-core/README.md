This crate is the unverified Rust prototype track of the `stella` project.

The source of truth for stella's theoretical foundations is `refs/originals/EngExegesis.pdf`.

This crate must never become the de-facto specification; it is an experimental implementation only.
